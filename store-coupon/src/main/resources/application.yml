spring:
  profiles:
    active: dev

  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.73.131:3306/store_sms
    driver-class-name: com.mysql.cj.jdbc.Driver

  application:
    name: store-coupon

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

server:
  port: 7000

information:
  msg: "nihao"
spring:


  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.73.132:3306/store_ums
    driver-class-name: com.mysql.cj.jdbc.Driver

  application:
    name: store-member

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 8000
